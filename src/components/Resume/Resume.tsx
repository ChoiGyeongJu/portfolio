import { NotionView } from 'common';
import { convertToUUID } from 'utils';

const Resume = () => {
  return <NotionView pageId={convertToUUID('4f495e9c4b604673bd193f87b159f05f')} />;
};

export default Resume;
